interface:
  display_name: "飞书邮箱助手 (Mac)"
  short_description: "Browse Feishu Mail in Edge and draft replies on macOS"
  default_prompt: "Use $feishu-mail-mac to browse my Feishu Mail inbox in Edge, read emails, and draft replies."

policy:
  allow_implicit_invocation: true

